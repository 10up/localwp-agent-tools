{
	"name": "localwp-mcp-server",
	"version": "1.0.0",
	"description": "MCP server for Local sites — provides WP-CLI, log, and config tools.",
	"main": "build/index.js",
	"type": "module",
	"scripts": {
		"build": "tsc",
		"watch": "tsc --watch"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.12.0",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.3.0"
	}
}
