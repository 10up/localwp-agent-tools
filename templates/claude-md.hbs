# {{siteName}}

WordPress site running at {{siteUrl}}

## Environment

| Setting    | Value          |
|------------|----------------|
| PHP        | {{phpVersion}} |
| MySQL      | {{mysqlVersion}} |
| Web Server | {{webServer}}  |
{{#unless (eq wpVersion "unknown")}}
| WordPress  | {{wpVersion}}  |
{{else}}
| WordPress  | unknown        |
{{/unless}}

{{#if multisite}}
## Multisite

This is a WordPress multisite installation using **{{multisiteType}}** mode.

{{/if}}
{{#if plugins.length}}
## Active Plugins

| Plugin | Version | Status |
|--------|---------|--------|
{{#each plugins}}
| {{this.name}} | {{this.version}} | {{this.status}} |
{{/each}}

{{/if}}
{{#if theme}}
## Active Theme

**{{theme.name}}** (v{{theme.version}})

{{/if}}
## File Structure

| Path | Description |
|------|-------------|
| `{{wpPath}}` | WordPress root directory |
| `{{wpPath}}/wp-content/themes/` | Theme files |
| `{{wpPath}}/wp-content/plugins/` | Plugin files |
| `{{wpPath}}/wp-content/uploads/` | Media uploads |
| `{{sitePath}}/logs/` | Server and PHP error logs |
| `{{wpPath}}/wp-config.php` | WordPress configuration |

## Development Notes

- The site domain is `{{domain}}` and resolves locally via Local's router.
- PHP error logs are at `{{sitePath}}/logs/php/error.log`.
- WordPress debug log (if enabled) is at `{{wpPath}}/wp-content/debug.log`.
- An MCP server is available with tools for querying the database, running WP-CLI commands, reading logs, and managing WordPress content. Use the available MCP tools rather than running shell commands directly when possible.
